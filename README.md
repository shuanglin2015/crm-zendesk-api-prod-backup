# Zendesk API

### Testing URL

* http://localhost:7071/api/getDataById?itemId=30327

### Credentials
create local.settings.json file in local root folder, search "crm-zendesk-api" in 1Password, it's in Shared "CRM" folder, copy the content to the local.settings.json file.